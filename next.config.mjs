/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'banner2.cleanpng.com',
      'banner2.cleanpng.com',
      'icon2.cleanpng.com',
      'lottie.host',
    ],
  },
};

export default nextConfig;
